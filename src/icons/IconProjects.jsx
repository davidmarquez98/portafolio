import FolderIcon from '@mui/icons-material/Folder';

function IconoProjects({ className }){
    return (<FolderIcon className={`icono-default__color light ${className}`}/>);
};

export default IconoProjects;